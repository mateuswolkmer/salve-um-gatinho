---
import { ButtonLink } from "../../Button";
import { Highlight } from "../../Highlight";
import PageSection from "../../PageSection.astro";
import RescueImage from '../../../assets/Outras/story/rescue.jpeg'
import TreatmentImage from '../../../assets/Outras/story/treatment.jpg'
import CommunityImage from '../../../assets/Outras/story/community.jpeg'
import { Image } from 'astro:assets';

---
<PageSection>
    <div class="flex flex-col gap-12 items-center max-w-5xl m-auto">
        <div class="flex flex-col md:flex-row gap-8 md:gap-16 w-full md:items-center">
            <div class="flex-1 min-h-40 max-h-40 rounded-xl flex items-end justify-center overflow-hidden relative border-black border-2">
                <Image src={RescueImage} alt='Gato resgatado' class='object-cover absolute -top-32' />
            </div>
            <p class="flex-1">Somos um grupo de voluntários que atuam no resgate de gatos em situação de maus tratos e vulnerabilidade</p>
        </div>
        <div class="flex flex-col md:flex-row-reverse gap-8 md:gap-16 w-full md:items-center">
            <div class="flex-1 w-full min-h-40 max-h-40 rounded-xl flex items-start justify-center overflow-hidden relative border-black border-2">
                <Image src={TreatmentImage} alt='Gato recebendo mamadeira' class='object-cover absolute -top-24' />
            </div>
            <p class="flex-1">Após estarem saudáveis e socializados, encaminhamos para <Highlight bold>adoção responsável</Highlight></p>
        </div>
        <div class="flex flex-col md:flex-row gap-8 md:gap-16 w-full md:items-center">
            <div class="flex-1 min-h-40 max-h-40 rounded-xl flex items-end justify-center overflow-hidden relative border-black border-2">
                <Image src={CommunityImage} alt='Gatos em gaiolinhas' class='object-cover absolute -top-[6.5rem]' />
            </div>
            <p class="flex-1">Contamos com o auxílio da comunidade para manter o abrigo e o tratamento dos animais</p>
        </div>
        <ButtonLink href="/sobre" variant="nav">Saiba mais</ButtonLink>
    </div>
</section>